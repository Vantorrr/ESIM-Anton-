# Шаг 3. Подключить token к admin API client

> [⬅️ Назад к фазе](../phase-3-admin-auth-and-api-security.md)

## Цель

Сделать отправку `Authorization` единым паттерном для admin requests.

## Что нужно сделать

- найти общий admin API wrapper;
- добавить bearer token injection;
- обработать `401/403` с понятным logout/re-login сценарием.

## Результат шага

Все admin write calls отправляют backend JWT.

## Статус

Не начато (Требует реализации)

## Журнал изменений

- Проведен первичный аудит (2026-05-07). Клиент API в админке в данный момент не работает с токенами `Authorization: Bearer`, так как JWT не запрашивается и не сохраняется.

## Файлы

- Клиентские сервисы админки (API wrappers/fetchers)

## Тестирование / Верификация

- Не проводилось (функционал отсутствует).
