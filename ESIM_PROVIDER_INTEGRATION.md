# 🔌 Интеграция с провайдерами eSIM

## Архитектура

Система поддерживает **два провайдера**:
- 🟢 **Основной** (Primary) - eSIM Go
- 🔵 **Резервный** (Fallback) - на выбор

При ошибке основного провайдера автоматически переключается на резервного.

---

## Настройка провайдеров

### 1. Основной провайдер (eSIM Go)

**API документация:** https://documenter.getpostman.com/view/19324374/2sA3kbgy28

В `.env` файле:
```env
ESIM_PRIMARY_API_URL="https://api.esimgo.com/v2"
ESIM_PRIMARY_API_KEY="YOUR_API_KEY_HERE"
```

**Получение ключа:**
1. Дождаться тестового кабинета от eSIM Go
2. Получить API ключ в личном кабинете
3. Вставить в `.env`

**Пока нет ключа:**
- Система работает в моковом режиме
- Можно тестировать весь флоу без реального API
- Когда получите ключ - просто вставьте в `.env` и перезапустите

---

### 2. Резервный провайдер (опционально)

Для отказоустойчивости можно настроить второго провайдера:

```env
ESIM_FALLBACK_API_URL="https://api.backup-provider.com"
ESIM_FALLBACK_API_KEY="BACKUP_API_KEY"
```

**Логика работы:**
1. Все запросы идут на основного провайдера
2. Если основной недоступен → автоматически переключается на резервного
3. В логах видно, какой провайдер обработал запрос

---

## Методы API провайдера

### 1. Получить список пакетов

**Endpoint:** `GET /api/esim-provider/packages`

**Query параметры:**
- `country` (опционально) - фильтр по стране

**Пример:**
```bash
curl http://localhost:3000/api/esim-provider/packages?country=USA
```

**Ответ:**
```json
[
  {
    "id": "usa_5gb_30d",
    "title": "USA 5GB / 30 days",
    "country": "USA",
    "dataAmount": "5GB",
    "validityDays": 30,
    "price": 15.99,
    "currency": "USD"
  }
]
```

---

### 2. Купить eSIM

**Endpoint:** `POST /api/esim-provider/purchase`

**Body:**
```json
{
  "packageId": "usa_5gb_30d",
  "email": "user@example.com"
}
```

**Ответ:**
```json
{
  "success": true,
  "order_id": "ESG123456",
  "iccid": "8901234567890123456",
  "qr_code": "data:image/png;base64,...",
  "activation_code": "LPA:1$smdp.esimgo.com$ACTIVATION_CODE",
  "status": "active"
}
```

---

### 3. Проверить статус заказа

**Endpoint:** `GET /api/esim-provider/orders/:orderId/status`

**Пример:**
```bash
curl http://localhost:3000/api/esim-provider/orders/ESG123456/status
```

**Ответ:**
```json
{
  "order_id": "ESG123456",
  "status": "active",
  "iccid": "8901234567890123456",
  "data_used": 1024,
  "data_total": 5120
}
```

---

### 4. Синхронизация продуктов

**Endpoint:** `POST /api/esim-provider/sync`

Загружает актуальные тарифы от провайдера и обновляет базу данных.

**Ответ:**
```json
{
  "synced": 45,
  "errors": 0
}
```

**Рекомендация:** Запускать раз в сутки (cron job).

---

### 5. Проверка здоровья провайдеров

**Endpoint:** `GET /api/esim-provider/health`

Проверяет доступность обоих провайдеров.

**Ответ:**
```json
{
  "primary": true,
  "fallback": true
}
```

---

## Интеграция в Orders Service

Когда заказ оплачен (webhook от ЮKassa), автоматически вызывается `fulfillOrder()`:

```typescript
// backend/src/modules/orders/orders.service.ts

async fulfillOrder(orderId: string) {
  const order = await this.findById(orderId);

  // Покупаем eSIM у провайдера
  const esimData = await this.esimProviderService.purchaseEsim(
    order.product.providerId,  // ID пакета у провайдера
    order.user.email           // Email пользователя
  );

  // Сохраняем данные eSIM в заказе
  await this.updateStatus(orderId, OrderStatus.COMPLETED, {
    qrCode: esimData.qr_code,
    iccid: esimData.iccid,
    activationCode: esimData.activation_code,
    providerOrderId: esimData.order_id,
  });

  // Начисляем кэшбэк, обновляем лояльность...
}
```

---

## Fallback логика (автоматическое переключение)

**Как это работает:**

1. Запрос идет на основного провайдера (eSIM Go)
2. Если ошибка (timeout, 500, недоступен):
   - Автоматически повторяет запрос на резервного
   - В логах пишется: `🔄 Переключение на резервного провайдера...`
3. Если резервный тоже не работает:
   - Возвращает ошибку пользователю

**Пример логов:**
```
[EsimProviderService] 📦 Запрос списка пакетов...
[EsimProviderService] ❌ Ошибка получения пакетов от основного провайдера: timeout
[EsimProviderService] 🔄 Переключение на резервного провайдера...
[EsimProviderService] ✅ Получено 45 пакетов от резервного
```

---

## Маппинг продуктов

### Откуда берутся продукты в БД?

**Вариант 1: Ручное добавление** (текущий)
- Админ создает продукты через админ-панель
- Указывает `providerId` (ID пакета у провайдера)

**Вариант 2: Автоматическая синхронизация**
- Вызываете `POST /api/esim-provider/sync`
- Система загружает все пакеты от провайдера
- Автоматически создает/обновляет продукты в БД
- Добавляет наценку (например, +50%)

**Рекомендация:** Использовать Вариант 2 + настроить cron.

---

## Тестирование без API ключа

Пока нет доступа к реальному API:

1. **Моковый режим включен автоматически**
   - Все методы работают с тестовыми данными
   - QR-коды генерируются локально

2. **Можно тестировать:**
   - Весь процесс покупки
   - Отправку QR-кодов пользователю
   - Начисление бонусов/кэшбэка
   - Админ-панель

3. **Когда получите API ключ:**
   - Вставляете в `.env`: `ESIM_PRIMARY_API_KEY="..."`
   - Перезапускаете: `pnpm dev`
   - Всё автоматически переключится на реальный API

---

## Мониторинг

### Логи

Все действия логируются:
```
✅ eSIM Provider сервис инициализирован
📦 Запрос списка пакетов...
✅ Получено 45 пакетов
💳 Покупка eSIM (package: usa_5gb_30d)...
✅ eSIM куплен успешно (order: ESG123456)
```

### Health Check

Проверка доступности провайдеров:
```bash
curl http://localhost:3000/api/esim-provider/health
```

**Настройте мониторинг** (например, UptimeRobot) для этого эндпоинта.

---

## Безопасность

✅ **API ключи в .env** (не коммитятся в Git)  
✅ **Таймауты** (30 секунд на запрос)  
✅ **Retry логика** (автоматический fallback)  
✅ **Логирование** всех действий  
✅ **Валидация** ответов от API  

---

## Roadmap

### Текущий статус
- [x] Архитектура готова
- [x] Поддержка двух провайдеров
- [x] Fallback логика
- [x] API endpoints
- [ ] Ждем тестовый доступ от eSIM Go
- [ ] Реальная интеграция (раскомментировать код)
- [ ] Автоматическая синхронизация продуктов

### Будущие улучшения
- [ ] Webhook от провайдера (статус активации)
- [ ] Кэширование списка пакетов (Redis)
- [ ] Rate limiting
- [ ] Статистика использования API

---

## FAQ

**Q: Можно ли запустить проект без API ключа?**  
A: Да! Система работает в моковом режиме.

**Q: Как добавить третьего провайдера?**  
A: Легко! Скопируйте логику fallback и добавьте tertiary client.

**Q: Что делать если оба провайдера упали?**  
A: Система вернет ошибку пользователю. Настройте алерты на health check.

**Q: Как обновить цены?**  
A: Запустите `POST /api/esim-provider/sync` - подтянет актуальные цены.

**Q: Какая наценка на тарифы?**  
A: Настраивается в коде синхронизации (по умолчанию +50%).

---

**Готово! Как получите API ключ - сразу заработает! 🚀**
