# 🚀 Инструкция по запуску проекта

## Предварительные требования

Убедитесь, что у вас установлено:
- **Node.js** 20+ ([скачать](https://nodejs.org/))
- **pnpm** ([установка](https://pnpm.io/installation)): `npm install -g pnpm`
- **Docker** ([скачать](https://www.docker.com/products/docker-desktop))
- **Git** ([скачать](https://git-scm.com/))

---

## Шаг 1: Подготовка окружения

### 1.1. Скопировать .env

```bash
cp .env.example .env
```

### 1.2. Создать Telegram бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. **Скопируйте токен** бота
5. Вставьте токен в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
   ```

### 1.3. Настроить остальные переменные (опционально на старте)

В `.env` можно оставить тестовые значения для **ЮKassa** и **ESIM_PROVIDER**.
Они будут работать в моковом режиме.

Реальные данные добавите позже, когда подключите интеграции.

---

## Шаг 2: Установка зависимостей

```bash
# Установить зависимости для всех модулей (monorepo)
pnpm install
```

---

## Шаг 3: Запуск базы данных

```bash
# Запустить PostgreSQL и Redis через Docker
docker-compose up -d

# Проверить, что контейнеры запущены
docker ps
```

Вы должны увидеть:
- `esim-postgres` (порт 5432)
- `esim-redis` (порт 6379)

---

## Шаг 4: Настройка базы данных

```bash
# Перейти в папку backend
cd backend

# Применить миграции (создать таблицы)
pnpm prisma migrate dev

# Заполнить БД тестовыми данными
pnpm prisma:seed

# (Опционально) Открыть Prisma Studio для просмотра данных
pnpm prisma studio
```

После выполнения `seed` вы получите:
- **Админа**: `admin@esim-service.com` / `admin123`
- **5 уровней лояльности** (Новичок → Платина)
- **18 тестовых продуктов** (eSIM для разных стран)

---

## Шаг 5: Запуск проекта

### Вариант A: Запустить все сразу (рекомендуется)

```bash
# Из корня проекта
pnpm dev
```

Это запустит:
- **Backend API** на `http://localhost:3000`
- **Admin Panel** на `http://localhost:3001`
- **Telegram Bot** (polling режим)

### Вариант B: Запустить модули по отдельности

**Терминал 1: Backend**
```bash
cd backend
pnpm dev
```

**Терминал 2: Admin**
```bash
cd admin
pnpm dev
```

**Терминал 3: Bot**
```bash
cd bot
pnpm dev
```

---

## Шаг 6: Проверка работы

### ✅ Backend API

Откройте: **http://localhost:3000/api/docs**

Вы увидите **Swagger документацию** со всеми эндпоинтами.

### ✅ Admin Panel

Откройте: **http://localhost:3001**

Вы увидите админ-панель с дизайном **Liquid Glass**.

Данные пока моковые (подключение к API будет позже).

### ✅ Telegram Bot

1. Откройте Telegram
2. Найдите вашего бота (username, который вы указали при создании)
3. Отправьте `/start`

Вы увидите главное меню с кнопками:
- 🌍 Каталог eSIM
- 👤 Мой профиль
- 📦 Мои заказы
- 🎁 Рефералы
- ❓ Помощь

---

## Шаг 7: Что дальше?

### 🔌 Подключить реальные интеграции

Когда получите доступы:

**ЮKassa:**
1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. Получите `SHOP_ID` и `SECRET_KEY`
3. Добавьте в `.env`
4. Настройте Webhook: `https://yourdomain.com/api/payments/webhook`

**eSIM Provider:**
1. Получите документацию API от провайдера
2. Реализуйте методы в `backend/src/modules/esim-provider/esim-provider.service.ts`
3. Добавьте `API_URL` и `API_KEY` в `.env`

---

## 📝 Полезные команды

```bash
# Установить новые зависимости
pnpm add <package>

# Создать новую миграцию БД
cd backend
pnpm prisma migrate dev --name migration_name

# Пересоздать БД (сбросить всё)
cd backend
pnpm prisma migrate reset

# Проверить код (lint)
pnpm lint

# Собрать production версию
pnpm build

# Запустить production
pnpm start
```

---

## 🐛 Решение проблем

### Проблема: "Port 3000 already in use"

```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows (PowerShell)
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process
```

### Проблема: "Cannot connect to database"

```bash
# Перезапустить Docker контейнеры
docker-compose down
docker-compose up -d

# Проверить логи
docker logs esim-postgres
```

### Проблема: "Bot not responding"

1. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
2. Проверьте логи бота в терминале
3. Попробуйте отправить `/start` снова

---

## 📞 Поддержка

Если возникли вопросы — пишите! 🚀
