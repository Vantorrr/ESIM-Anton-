# 🏗 Архитектура проекта

## Общая схема

```
┌──────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТСКАЯ ЧАСТЬ                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────┐         ┌──────────────────┐               │
│  │  Telegram Bot   │         │  Admin Panel     │               │
│  │  + Mini App     │         │  (Next.js 15)    │               │
│  │  (Grammy)       │         │  Liquid Glass UI │               │
│  └────────┬────────┘         └────────┬─────────┘               │
│           │                           │                          │
└───────────┼───────────────────────────┼──────────────────────────┘
            │                           │
            │         REST API          │
            └───────────┬───────────────┘
                        │
┌───────────────────────▼───────────────────────────────────────────┐
│                      BACKEND (NestJS)                              │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  Users   │  │ Products │  │  Orders  │  │ Payments │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │Referrals │  │ Loyalty  │  │Analytics │  │   Auth   │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
│                                                                    │
│  ┌────────────────────────────────────────────────────┐           │
│  │         eSIM Provider Integration (Mock)           │           │
│  └────────────────────────────────────────────────────┘           │
│                                                                    │
└────────────────────────┬───────────────────────────────────────────┘
                         │
         ┌───────────────┴────────────────┐
         │                                │
┌────────▼──────┐              ┌──────────▼────────┐
│  PostgreSQL   │              │      Redis        │
│  (Prisma ORM) │              │  (Cache + Queue)  │
└───────────────┘              └───────────────────┘
         │
         │
┌────────▼─────────────────────────────────────────────┐
│            ВНЕШНИЕ ИНТЕГРАЦИИ                        │
├──────────────────────────────────────────────────────┤
│  • ЮKassa (Payments)                                 │
│  • eSIM Provider API (Mock → Real)                   │
│  • Telegram API                                      │
└──────────────────────────────────────────────────────┘
```

---

## Технологический стек

### Backend (API)
- **Framework**: NestJS (Node.js + TypeScript)
- **Database**: PostgreSQL 16
- **ORM**: Prisma
- **Cache**: Redis
- **API Docs**: Swagger
- **Auth**: JWT
- **Validation**: class-validator

### Admin Panel
- **Framework**: Next.js 15 (App Router)
- **UI**: React 19 + TypeScript
- **Styling**: TailwindCSS
- **Design**: Liquid Glass (Glassmorphism)
- **State**: React Query
- **Charts**: Recharts

### Telegram Bot
- **Framework**: Grammy (современная библиотека)
- **Language**: TypeScript
- **Mini App**: React (будет реализовано)

---

## Структура базы данных

```
users
├── id (PK)
├── telegramId (unique)
├── balance
├── bonusBalance
├── referralCode
├── referredById (FK → users)
└── loyaltyLevelId (FK → loyalty_levels)

esim_products
├── id (PK)
├── country
├── name
├── dataAmount
├── validityDays
├── providerPrice
├── ourPrice
└── providerId

orders
├── id (PK)
├── userId (FK → users)
├── productId (FK → esim_products)
├── status (enum)
├── totalAmount
├── qrCode
├── iccid
└── activationCode

transactions
├── id (PK)
├── userId (FK → users)
├── orderId (FK → orders)
├── type (enum)
├── status (enum)
├── amount
└── paymentId

loyalty_levels
├── id (PK)
├── name
├── minSpent
├── cashbackPercent
└── discount

admins
├── id (PK)
├── email (unique)
├── password (bcrypt)
└── role (enum)
```

---

## Модули Backend

### 1. **Users Module**
- Управление пользователями
- Регистрация через Telegram
- Баланс и бонусы
- Статистика

### 2. **Products Module**
- Каталог тарифов eSIM
- CRUD операции
- Синхронизация с провайдером

### 3. **Orders Module**
- Создание заказов
- Обработка статусов
- Выдача eSIM

### 4. **Payments Module**
- Интеграция с ЮKassa
- Webhook обработка
- История транзакций

### 5. **Referrals Module**
- Реферальная программа
- Начисление бонусов
- Статистика

### 6. **Loyalty Module**
- Уровни лояльности
- Кэшбэк
- Скидки

### 7. **Analytics Module**
- Дашборд
- Отчеты
- Графики продаж

### 8. **Auth Module**
- JWT аутентификация
- Админ-доступ

---

## Процесс покупки eSIM

```
1. Пользователь выбирает тариф в боте
   ↓
2. Создается Order (status: PENDING)
   ↓
3. Генерируется платеж в ЮKassa
   ↓
4. Пользователь оплачивает
   ↓
5. Webhook от ЮKassa → Order (status: PAID)
   ↓
6. Запрос к провайдеру eSIM
   ↓
7. Получение QR-кода и данных
   ↓
8. Order (status: COMPLETED)
   ↓
9. Отправка QR-кода пользователю в боте
   ↓
10. Начисление кэшбэка и реферальных бонусов
```

---

## Масштабирование

### Horizontal Scaling
- Backend API легко масштабируется (stateless)
- Load Balancer (Nginx)
- Несколько инстансов через PM2 cluster mode

### Database Optimization
- Индексы на часто запрашиваемых полях
- Connection pooling (Prisma)
- Redis для кэширования

### Queues (BullMQ + Redis)
- Обработка платежей
- Отправка уведомлений
- Синхронизация с провайдером

---

## Безопасность

✅ **Реализовано:**
- JWT токены
- bcrypt для паролей
- CORS защита
- Валидация входных данных

✅ **TODO (для production):**
- Rate limiting
- HTTPS (SSL)
- Webhook signature verification
- Input sanitization

---

## Мониторинг

**Logs:**
- NestJS Logger
- PM2 logs
- Nginx access/error logs

**Metrics:**
- PM2 monitoring
- Prisma query performance
- Redis stats

**Alerts:**
- PM2 email notifications
- UptimeRobot для проверки доступности

---

## CI/CD (рекомендуется)

```yaml
# .github/workflows/deploy.yml
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - Checkout code
      - Install dependencies
      - Run tests
      - Build
      - Deploy to VPS via SSH
```

---

**Архитектура спроектирована для:**
- ⚡️ Высокой производительности
- 📈 Легкого масштабирования
- 🔧 Простоты поддержки
- 🚀 Быстрого добавления новых фич
